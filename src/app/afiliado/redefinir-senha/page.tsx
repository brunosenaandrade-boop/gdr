import { RedefinirSenhaClient } from "./client";

export const metadata = {
  title: "Redefinir senha — Afiliado",
  robots: "noindex, nofollow",
};

export default function AfiliadoRedefinirSenhaPage() {
  return <RedefinirSenhaClient />;
}
